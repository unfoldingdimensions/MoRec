# P0 feature agent prompts

One prompt per session/PR. Copy verbatim into a fresh agent chat. Each assumes the repo is checked out at `E:\New-Personal-Projects\MoRec` on Windows, `main` is up to date, and `docs/plans/2026-09-18-p0-features.md` (the implementation spec these reference) is on main.

---

## Prompt 1 — Motion presets ("make the magic optional")

You are implementing a feature in the MoRec repo (E:\New-Personal-Projects\MoRec, Electron + React + TS, Vite/vitest/biome). Mission: **motion presets + "make the magic optional" controls**, exactly as specified in `docs/plans/2026-09-18-p0-features.md` → "Feature 1". Read first, in order: `AGENTS.md`, `CONTRIBUTING.md`, that plan file (Feature 1 section + "Sequencing & rules"), and `src/components/video-editor/timeline/zoomSuggestionUtils.ts` + `timeline/hooks/actions/useTimelineZoomActions.ts` to understand the suggestion pipeline you are capping.

Deliverables: a persisted four-level motion profile (off/subtle/balanced/energetic) in `editorPreferences.ts`; suggested-zoom depth clamped by the profile (manual zoom regions never clamped); `disableSuggestedZooms` derived from the profile; a segmented control in the SettingsPanel zoom section; a real slider for zoom depth in the zoom region inspector if it is currently stepped-only. New logic goes in a pure module `src/components/video-editor/motionProfile.ts` with unit tests; extend `editorPreferences.test.ts` and the zoom-actions tests. Every new UI string needs keys in all 11 locale files (`npm run i18n:check` must pass — it enforces parity and will fail otherwise).

Process: branch `feat/motion-presets` off latest main; conventional commits; verify with actual output of `npm test`, `npm run lint`, `npm run i18n:check`; open one PR to main and merge only with CI green. Environment notes: a Mimosa security hook blocks raw `git commit`/`git push` in Bash — write the git commands into a `.tmp/<name>.sh` file with the Write tool and run `sh .tmp/<name>.sh` (approved pattern; do not "fix" the pre-existing security advisories). Keep changes out of `VideoEditor.tsx` except minimal glue. Out of scope: silence/speed detection, export changes, anything in the plan marked Session 2-4. Definition of done: plan Feature 1 checklist complete, tests written and passing, CI green, PR merged.

---

## Prompt 2 — Smart regions: silence removal + dead-air speed-up

You are implementing two related features in the MoRec repo (E:\New-Personal-Projects\MoRec). Mission: **one-click silence removal (trims) and auto speed-up of typing/dead air (speed regions)**, exactly per `docs/plans/2026-09-18-p0-features.md` → "Feature 2" and "Feature 3". Read first: `AGENTS.md`, `CONTRIBUTING.md`, the plan (both sections + "Sequencing & rules"), then `src/components/video-editor/timeline/zoomSuggestionUtils.ts` (the pattern to mirror for suggestion modules), `src/components/video-editor/types.ts` region types (TrimRegion, SpeedRegion, mapSourceTimeToTimelineTime), `electron/ipc/captions/generate.ts` (`extractCaptionAudioSource` — the audio-extraction pattern), `electron/ipc/captions/silence.ts` (silencedetect constants), and `electron/ipc/cursor/interaction.ts` (uiohook capture — for the optional keyboard source).

Deliverables: (1) pure suggestion modules `src/components/video-editor/timeline/silenceSuggestions.ts` and `typingSuggestions.ts` with the guards specified in the plan (padding, merge gap, max-removal ratio, reservations against existing zoom/speed/trim spans, fast-check invariants); (2) new IPC `analyze-companion-audio-silence` registered in `electron/ipc/register/recording.ts` following the `generate-auto-captions` pattern (companion audio via `getUsableCompanionAudioCandidates`, ffmpeg silencedetect via the existing spawn wrappers — do NOT refactor the advisory-flagged spawn sites); (3) editor toolbar buttons "Remove dead air" / "Speed up typing" / "Speed up dead air" wired into VideoEditor.tsx glue that create TrimRegions/SpeedRegions (full undo comes free via editorHistory); (4) if time allows, keyboard telemetry: uiohook `keydown` listener, `interactionType: "key"`, `CURSOR_TELEMETRY_VERSION` 2→3 with backward-compatible readers (v2 payloads must still load) — version bump + normalization whitelist per the plan.

Process: branch `feat/smart-regions` off latest main; conventional commits; `npm test`, `npm run lint`, `npm run i18n:check` must pass with real output; one PR, merge with CI green. Mimosa hook: use the `.tmp/<name>.sh` script pattern for git commit/push (see the plan's "Sequencing & rules"). Out of scope: export-side changes (regions already flow through every export route — do not touch export args), motion presets, export reliability pack. Definition of done: both features' test matrices per the plan, CI green, PR merged.

---

## Prompt 3 — Export reliability pack

You are implementing export-resilience features in the MoRec repo (E:\New-Personal-Projects\MoRec). Mission: **target file-size preset + resumable Lightning (native static-layout) export**, exactly per `docs/plans/2026-09-18-p0-features.md` → "Feature 4" (4a and 4b). Read first: `AGENTS.md`, `CONTRIBUTING.md`, the plan (Feature 4 + "Sequencing & rules"), then `src/lib/exporter/exportBitrate.ts`, `src/lib/exporter/finalizationTimeout.ts`, `src/components/video-editor/ExportSettingsMenu.tsx`, `src/components/video-editor/exportStatusModel.ts`, and `electron/ipc/export/native-video.ts` — specifically `buildNativeStaticLayoutTimelineSegments` (~L1715), `buildNativeStaticLayoutChunks` (~L684), `exportNativeStaticLayoutVideo` (~L3208), and the existing cancel/session plumbing (`native-static-layout-export-cancel`, `nativeStaticLayoutExportSessions`). Work carefully in native-video.ts: it carries known, accepted security advisories — do not refactor spawn sites; follow existing arg-builder style.

Deliverables 4a: `target-size` quality mode in exportBitrate (bitrate from duration + target MB, MIN_MP4_BITRATE clamp, container overhead factor), persisted via ProjectEditorState, MB input + live estimated-size readout in ExportSettingsMenu, consumed by both native and WebCodecs routes. Deliverables 4b: resumable static-layout export only — per-export temp session dir (`<temp>/export-session/<exportId>/`) with an atomically-written manifest (`electron/ipc/project/atomicSave.ts` pattern) tracking rendered segments; segment loop skips done segments; on failure/cancel keep the dir and surface a resume hint via `exportStatusModel`; resume flow through `resolveExportStartSettings` with a settings-hash match; 7-day sweep for stale session dirs; `segment i/N` in the runtime label. WebCodecs/breeze routes: explicitly non-resumable (surface as skip reason). Cancel semantics are already fixed (cancel = success-with-flag) — do not regress them.

Process: branch `feat/export-reliability` off latest main; conventional commits; `npm test`, `npm run lint`, `npm run i18n:check` with real output; one PR, merge with CI green. Mimosa hook: `.tmp/<name>.sh` script pattern for git commit/push. Out of scope: smart regions, motion presets, GIF/canvas, and any change to WebCodecs resumability. Definition of done: 4a + 4b test matrices per the plan (arg-builder tests for skip-done-segments, manifest round-trip, bitrate math, resume banner logic), CI green, PR merged.

---

## Prompt 4 — Audio pre-flight + social canvas presets

You are implementing two UI-facing features in the MoRec repo (E:\New-Personal-Projects\MoRec). Mission: **audio pre-flight check + social canvas presets**, exactly per `docs/plans/2026-09-18-p0-features.md` → "Feature 5" and "Feature 6". Read first: `AGENTS.md`, `CONTRIBUTING.md`, the plan (both sections + "Sequencing & rules"), then `src/hooks/useAudioLevelMeter.ts` (+ its test), `src/components/launch/popovers/MicPopover.tsx` and `PopoverScaffold.tsx` (MicDeviceRow), `src/components/video-editor/exportDimensions.ts`, `ExportSettingsMenu.tsx`, and `src/lib/exporter/types.ts`.

Deliverables 5: pre-flight warnings in the mic popover — clipping warning (sustained high RMS) and no-input warning (silence while popover open) from the existing meter; honest system-audio state chip; post-recording silent-companion diagnostic toast via a cheap ffmpeg peak probe on `<video>.system.wav`/`<video>.mic.wav` discovered through `getUsableCompanionAudioCandidates` (electron/ipc/recording/diagnostics.ts) — do not attempt browser loopback metering; non-blocking warnings only (never block recording start). Deliverables 6: `ExportCanvas = "original"|"9:16"|"1:1"|"4:5"` in exportDimensions.ts (center-crop after quality scaling, even-normalized; canvas applies after zoom), segmented canvas picker with live dimensions in ExportSettingsMenu, persisted `exportCanvas` in ProjectEditorState, honored in the native static-layout args and the WebCodecs frame renderer crop, and composed with GIF size presets (canvas first, then size preset).

Process: branch `feat/preflight-and-canvas` off latest main; conventional commits; `npm test`, `npm run lint`, `npm run i18n:check` with real output; one PR, merge with CI green. Mimosa hook: `.tmp/<name>.sh` script pattern for git commit/push. Out of scope: everything from Sessions 1-3, WASAPI loopback metering (v1.1 stretch only if trivially available), recording-start gating. Definition of done: both features' tests per the plan (meter thresholds, peak-probe parsing, canvas×quality dimension matrix, renderer crop offset), CI green, PR merged.
